/**
 * 关键词提取 — Prompt 模板
 *
 * 将中文场景文本转换为英文搜索关键词，用于 Unsplash/Pexels 图库检索。
 */

/** 单条提取：一次处理一个场景文本 */
export const KEYWORD_EXTRACTION_SYSTEM =
  '你是搜索关键词专家。从用户文本中提取1-3个英文关键词（名词或形容词+名词），用于图片搜索。只输出关键词，空格分隔，不要其他内容。';

/** 批量提取：一次处理所有场景，返回 JSON 数组 */
export const BATCH_KEYWORD_EXTRACTION_SYSTEM = `你是搜索关键词专家。用户将提供多个场景文本（按数字编号）。
请为每个场景提取 1-3 个英文关键词（名词或形容词+名词），用于图片搜索。

**必须严格按顺序返回一个 JSON 数组**，格式如下：
["场景1关键词", "场景2关键词", "场景3关键词", ...]

只输出 JSON 数组，不要包含其他任何内容。`;
